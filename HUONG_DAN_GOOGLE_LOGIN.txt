═══════════════════════════════════════════════════════════════
  HƯỚNG DẪN THIẾT LẬP ĐĂNG NHẬP BẰNG GOOGLE - CHỈ 3 BƯỚC
═══════════════════════════════════════════════════════════════

📌 BƯỚC 1: Lấy Google OAuth Credentials (3 phút)
─────────────────────────────────────────────────────────────

1. Mở trình duyệt, vào: https://console.cloud.google.com/

2. Tạo Project:
   - Click "Select a project" > "New Project"
   - Tên: JobFinder > "Create"

3. Bật API:
   - Menu ☰ > APIs & Services > Library
   - Tìm "People API" > Click "Enable"

4. Tạo OAuth Credentials:
   
   ⚠️ QUAN TRỌNG: Phải cấu hình Consent Screen TRƯỚC!
   
   A. Cấu hình Consent Screen (BẮT BUỘC):
      - Khi thấy "To create an OAuth client ID, you must first configure your consent screen"
      - Click vào nút "CONFIGURE CONSENT SCREEN" (màu xanh)
      - Hoặc vào: APIs & Services > OAuth consent screen
      
      - Nếu thấy "Google Auth Platform not configured yet":
        * Click "GET STARTED" hoặc "EDIT APP" (nút màu xanh)
      
      - Bước 1: App Information
        * App name: Nhập "JobFinder" (BẮT BUỘC - không được để trống)
        * User support email: Chọn email của bạn từ dropdown
        * Click "NEXT" hoặc "SAVE AND CONTINUE" ở dưới cùng
      
      - Bước 2: Audience
        * Chọn "External" (cho phép bất kỳ ai dùng Google để đăng nhập)
        * Click "CREATE" hoặc "NEXT"
      
      - Bước 3: Contact Information
        * Developer contact information: Nhập email của bạn
        * Click "SAVE AND CONTINUE" hoặc "NEXT"
      
      - Bước 4: Finish
        * Xem lại thông tin
        * Click "BACK TO DASHBOARD" hoặc "DONE"
        * Hoặc đóng tab này và quay lại trang Credentials
   
   B. Tạo OAuth Client ID:
      - Quay lại: APIs & Services > Credentials
      - Click "+ CREATE CREDENTIALS" > "OAuth client ID"
      - Application type: Chọn "Web application"
      - Name: JobFinder
      - Authorized JavaScript origins: Click "ADD URI" và thêm:
        + http://localhost:3000
        + http://localhost:8080
      - Authorized redirect URIs: Click "ADD URI" và thêm:
        + http://localhost:8080/auth/google/callback
      - Click "CREATE"
   
5. COPY 2 thông tin này:
   ✅ Client ID: (dạng: 123456789-abc...xyz.apps.googleusercontent.com)
   ✅ Client Secret: (dạng: GOCSPX-abc...xyz)


📌 BƯỚC 2: Điền vào file .env (1 phút)
─────────────────────────────────────────────────────────────

1. Mở file: server/.env

2. Tìm 2 dòng này:
   GOOGLE_CLIENT_ID=your-google-client-id-here
   GOOGLE_CLIENT_SECRET=your-google-client-secret-here

3. Thay thế bằng giá trị bạn vừa copy:
   GOOGLE_CLIENT_ID=123456789-abcdefghijklmnop.apps.googleusercontent.com
   GOOGLE_CLIENT_SECRET=GOCSPX-abcdefghijklmnopqrstuvwxyz

4. Lưu file


📌 BƯỚC 3: Restart Server (30 giây)
─────────────────────────────────────────────────────────────

1. Dừng server (Ctrl+C trong terminal đang chạy server)

2. Chạy lại:
   cd server
   npm run dev

3. Bạn sẽ thấy: "✅ Google OAuth Strategy initialized"


✅ XONG! Bây giờ vào http://localhost:3000/login và click 
   "Đăng nhập bằng Google" để test!


═══════════════════════════════════════════════════════════════
  NẾU GẶP LỖI:
═══════════════════════════════════════════════════════════════

❌ "redirect_uri_mismatch"
   → Kiểm tra lại Authorized redirect URIs trong Google Console
   → Phải chính xác: http://localhost:8080/auth/google/callback

❌ "invalid_client"  
   → Kiểm tra lại GOOGLE_CLIENT_ID và GOOGLE_CLIENT_SECRET
   → Đảm bảo không có khoảng trắng thừa
   → Restart server sau khi sửa .env

❌ "OAuth2Strategy requires a clientID option"
   → File .env chưa có GOOGLE_CLIENT_ID hoặc giá trị sai
   → Kiểm tra lại file server/.env

═══════════════════════════════════════════════════════════════
